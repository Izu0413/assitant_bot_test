# TASKS.md — 合意済みバックログ(2026-08-05時点)

締切から逆算: エージェント提出 8/17 08:59 JST(残り12日)。
着手順: **A → 0 → 1 → 2 → 3 →(余力で)4 → 5 → 6**。
各タスクは「実装 → arena 400戦 → experiments.csv記録 → 昇格判定」で1サイクル。

---

## タスクA: v2.1の採用判定(最初にやる)— ✅完了 2026-08-05: 判定「差なし」・v2.1保留

結果(再構築基盤での測定。experiments.csv 参照):
- ミラー400戦: v2.1勝率 48.5% [43.6, 53.4] — CIが50%を跨ぎ分離せず。
  旧175戦の53.1%はノイズ範囲だったと解釈。
- 非ミラー(grimmsnarl優先度ボット相手に各400戦): v1 94.5% / v2.1 94.5% — 差ゼロ。
  ただし相手が弱すぎて94.5%の天井に張り付いており、この測定の検出力は低い
  (差を見るには互角に近い非ミラー相手が必要。タスク5のメタ採掘後に再測定推奨)。
- 判定: 完了条件のとおり「差なし」と記録し、v2.1は保留。チャンピオンはv1のまま。タスク1へ。

v2.1(agents_final_v2)はミラー175戦で53.1%[45.8,60.4]。判定未了。

1. ミラー400戦: `python arena.py agents_final_v2/main.py agents_final/main.py -n 400 --note "v2.1採用判定"`
2. 非ミラー検証: meta_decks.py の grimmsnarl リストを持たせた汎用優先度ボットを
   作り(旧agent/main.py の優先度ロジック+grimmsnarlデッキで可)、
   v1とv2.1をそれぞれ400戦ずつ当てて勝率差を見る。
   ※ミラーはv2のサイドレート重みが打ち消される環境なので、こちらが本命の判定。
- **完了条件**: どちらかでCIが50%から分離 → 勝者をチャンピオンに昇格し
  agents_final/ を更新。分離しなければ「差なし」と記録してv2.1は保留、タスク1へ。

## タスク0: Kaggle本番デプロイの確証

- 提出tar.gzビルドスクリプト `build_submission.sh` を作る
  (CLAUDE.mdのチェックリスト準拠。libcg.so は
  `python -c "import kaggle_environments as k,os;print(os.path.dirname(k.__file__))"`
  配下の envs/cabt/cg/libcg.so をコピー)。
- main.py に一度だけ `_SEARCH_READY` をstderrへprintする行を入れる(デッキ提出時)。
- **完了条件**: tarが手元で `kaggle_environments.make("cabt").run([展開先/main.py,"random"])`
  相当のスモークを通る。※提出とエピソードstderrの目視は人間タスク。

## タスク1: 探索の時間管理と量スケール — ✅完了 2026-08-05: 昇格

結果(experiments.csv 参照):
- 実装: サンプル3→適応上限30(h1維持)。予算=残時間×0.6÷残想定判断数
  (初期40、判断ごとに減衰、下限12)、1判断上限8s。1サンプルごとに
  search_begin/end で完結させ、予算超過時は途中平均で打ち切り。
- 実測分布: 30サンプル・平均210〜340ms/判断・最大1.28s、
  1試合の探索合計14〜20.5s(予算600sの3.4%)。TIMEOUT/INVALID/ERROR 0件。
- A/B vs 旧v1: 初回400戦 54.5%[49.6,59.3](僅差跨ぎ)→追加400戦 56.5%[51.6,61.3]
  → プール800戦 55.5%[52.0,58.9] でCI分離 → 昇格。
- 旧チャンピオン(v1)は agents_archive/v1_20260805/ に退避(ladder相手として保持)。
- 残余地: 上限30でも予算の3.4%。さらに増やす(横=サンプル)か
  タスク2(縦=適用範囲拡張)が次のレバー。

現状: サンプル3×h1固定、1試合の探索≈1秒(予算600秒の0.2%)。
- `remainingOverageTime` から1手の時間予算を計算
  (例: 残時間×0.6 ÷ 残り想定判断数(初期値40、経過で減衰))。
- 予算内でサンプル数を適応的に増やす(3→上限30)。予算が厚ければ horizon=2。
- セーフティ: 予算超過しそうなら途中サンプルで打ち切り平均。MIN_OVERAGE_FOR_SEARCH維持。
- **完了条件**: ローカル400戦でTIMEOUT/INVALID 0件、1試合の探索時間の実測分布を
  記録、現行チャンピオンに対しCI分離の勝ち越し。

## タスク2: 探索の適用範囲をサブ選択へ拡張

現状: SELECT_MAIN のみ探索。昇格(倒された後の後続選択)とBoss標的がルール直感。
- `_search_decide_main` を一般化し、コンテキストのホワイトリストで適用:
  まず `CTX_TO_ACTIVE`(自分の昇格/Boss標的の両方)、次に `CTX_DISCARD`、`CTX_MULLIGAN`。
- 候補数が多い選択は上位k候補に絞ってから探索(ルール採点でプレフィルタ)。
- **完了条件**: 適用コンテキストごとにON/OFFのA/B 400戦を記録。合計でCI分離の改善。

## タスク3: 初手の場作り(SETUP)の意思決定

現状: `CTX_SETUP_ACTIVE` は汎用フォールバック=**手札の並び順**で先頭を出している。
SETUP_BENCH も先頭から2体。
- リード選択のランキングを実装: Dwebbleリード(1枚サイド+Ascension始動) vs
  Kangaskhanリード(ドロー即稼働だが3枚サイド露出)。まず固定方針+A/Bで決める。
  余力があればタスク2の機構でSETUPも探索対象に。
- **完了条件**: 両リード方針のA/B 400戦を記録し、勝った方を既定に。

## タスク4: エネルギーの色ルーティング

- 支払い(にげる/ワザコスト)の捨て順優先度をカードIDで実装:
  草はCrustle系が場にいる限り温存、壁に付いたSpikyは最後、Mistは中間。
- 装着側: 草はCrustle/Dwebble優先(壁割りのSuperb Scissorsは{草}◯◯)。
- **完了条件**: A/B 400戦記録。悪化しないこと(中立なら採用可、コスト低のため)。

## タスク5: ロールアウト内の相手モデル汎用化 + meta_decks拡充

- rules_decide の未知カード既定点を、カードDB駆動の汎用方策に置換:
  ドロー系サポートは使う、アクティブのワザコストに向けてエネを付ける、
  進化できるならする、程度で十分。
- kaggle_scrape.py → meta_analyze.py を回して上位デッキを採掘し、
  ARCHETYPE_SIGNS/DECKS を3型→N型へ(週次)。レポートのメタ分析章と兼用。
- **完了条件**: 非ミラー相手のA/Bで改善、meta_decksが5型以上。

## タスク6: コイン分散の制御(余力があれば)

- Rapid-Fireのコインがロールアウトのノイズ源。search_begin の manual_coin
  (現在0固定)の挙動を調査し、期待値化 or 固定化でサンプル収束を速くする。
- **完了条件**: 同一サンプル数での候補スコア分散が下がることを計測で示す。

---

## 常設ルール

- 昇格したら旧チャンピオンは agents_archive/ に日付付きで退避(ladder相手として保持)。
- 8/16-17: その時点の最強2体を最終提出(直近2提出のみが最終評価対象)。
- どの実験も experiments.csv に note付きで記録。negative resultも書く。
